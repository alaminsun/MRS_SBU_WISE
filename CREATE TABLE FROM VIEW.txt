CREATE TABLE DB_USER_WORKING_SESSION
AS(SELECT * FROM VW_USER_WORKING_SESSION)

CREATE TABLE DB_PRESCRIPTION_MASTER
AS(SELECT * FROM VW_PRESCRIPTION_MASTER)

CREATE TABLE DB_PRESCRIPTION_DETAIL
AS(SELECT * FROM VW_PRESCRIPTION_DETAIL)